#include <stdio.h>
int main() {
	int num,g,s,b;
	scanf("%d",&num);
	g=num%10;
	b=num/100;
	s=(num/10) % 10;
	while(b) {
		printf("B");
		b--;
	}
	while(s) {
		printf("S");
		s--;
	}
	int i=1;
	while(g) {
		printf("%d",i++);
		g--;
	}
	return 0;
}
